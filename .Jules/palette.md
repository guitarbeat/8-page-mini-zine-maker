## 2024-05-24 - Progress Bar Accessibility
**Learning:** Adding `role="progressbar"` and `aria-valuenow` to custom progress indicators is crucial for screen readers to announce progress updates. Linking the status text via `aria-describedby` provides context.
**Action:** Always inspect custom components for semantic roles and ARIA attributes, especially dynamic ones like progress bars.
