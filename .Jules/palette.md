# Palette's Journal - Critical Learnings

## 2024-05-22 - Initial Setup
**Learning:** Project uses manual CSS variables and utility classes instead of Tailwind build.
**Action:** When adding styles, define them in `assets/styles.css` using the existing variable system.
